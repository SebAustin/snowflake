name: dbt_snowflake_project
version: '1.0.0'
config-version: 2

profile: dbt_snowflake_project

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_snowflake_project:
    staging:
      +materialized: view
    marts:
      +materialized: table